// (Color of code) - to help us spot keyworrds and make the code more readable.

// (camelCase)- Developers love it for labels (you can't have spaces/symbols so makes it easier to read)

// Semicolons - it's best to end each line with them.

//(Errors) - These might be intimidating but we will practice udnerstanding them. Use them to find to level up.

//(Syntax) - Rules/Structure of how code is written (grammer for how we write code)

// (The Console) The console is a tool we as developers use to peek into JS and see what the values are of variables are at specific times while running code.